
import { subStr } from 'message-queue-service/helpers/sub-str';
import { module, test } from 'qunit';

module('Unit | Helper | sub str');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subStr([42]);
  assert.ok(result);
});

